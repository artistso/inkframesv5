package com.inkframe.feature.layers

/**
 * Placeholder for the layers feature module.
 *
 * The layer panel currently lives inline in `:feature-canvas` (`StudioScreen`'s
 * `SidePanel`). This module is reserved for extracting a full layer manager: reordering,
 * grouping/folders, blend-mode & opacity controls, layer thumbnails, and clipping masks.
 */
internal object LayersModule
